bin/
obj/
*.user
.vs/

# Local secrets that aren't managed through `dotnet user-secrets` must never be committed.
secrets.json
*.secrets.json
appsettings.*.local.json

dry-run-*.json
applications.json
cursors.json
run-reports.json
approved-sources.json
